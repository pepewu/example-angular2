export interface TripChapter {
    orderIndex?: number,
    title?: string,
    type?: string,
    startDate?: string,
    startTimestamp?: string,
    endDate?: string,
    budget?: number,
    data?: any
    places?: any[],
    events?: any[],
    costs?: any[]
}